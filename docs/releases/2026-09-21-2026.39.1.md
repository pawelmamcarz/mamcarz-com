# Wydanie 2026.39.1

Status dokumentu: przygotowany przy cięciu wydania 2026-09-21. Ten dokument nie zatwierdza pushu na `main`, merge, wdrożenia Pages, wdrożenia Workera ani zmiany konfiguracji konta Cloudflare.

## 1. Zakres

- Kanoniczna wersja produktu: `2026.39.1` w `version.js`.
- Token cache-bust wspólnych zasobów: `20260921-2026.39.1` (osobny od `VERSION`, powiązany z tym wydaniem).
- `CHANGELOG.md` z perspektywy właściciela strony.
- Bez zmian treści merytorycznej stron poza query `?v=` przy `style.css` i `main.js`.

Stan `main` przed tym PR: `993ccdd` (Merge pull request #14, TypeSafe Jev).
Commit cięcia na branchu: `1f05510`.

## 2. Bramki

| Bramka | Stan |
| --- | --- |
| Commit wydania na branchu PR | tak, ten PR |
| Push na `main` | nie |
| Merge do `main` | nie |
| Cloudflare Pages deploy | nie |
| Worker deploy | nie w tym cięciu; Jev z PR #14 był wdrażany osobno wcześniej |
| Live Workers AI / Jev | nie wywoływane w tym strumieniu |

## 3. Weryfikacja lokalna

Uruchomione przy cięciu:

- `npm run test:verify-site`
- `npm run verify:site`
- `cmp -s AGENTS.md CLAUDE.md`
- `node --check` na ruszonych plikach JS (`version.js`, `scripts/verify-site.mjs`)

Wyniki wpisz po przebiegu w tym samym PR. Nie traktuj zielonych testów jako zgody na deploy.

## 4. Osobne decyzje publikacyjne

Każdy punkt wymaga osobnego zatwierdzenia:

1. merge tego PR do `main`;
2. deploy Cloudflare Pages z zatwierdzonego, czystego commita:

   ```bash
   wrangler pages deploy . --project-name mamcarz-com --branch main --commit-dirty=true
   ```

3. osobny deploy Workera tylko jeśli produkcja nie ma jeszcze Jev z PR #14:

   ```bash
   wrangler deploy --config worker/wrangler.toml
   ```

4. potwierdzenie Bulk Redirect `www` → apex (`301`, ścieżka i query zachowane);
5. powdrożeniowe sprawdzenie, że strony serwują `style.css?v=20260921-2026.39.1` i `main.js?v=20260921-2026.39.1`.
